<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Tech Blog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>

<body class="bg-primary-subtle">
    <div class="">
        <header class="bg-secondary p-2">
            <nav class="">
                <!-- Conditionally render login or logout links -->
                <ul class="list-group list-group-flush d-flex flex-row border-0">
                    <li class="list-group-item bg-secondary border-0">
                        <a class="text-light link-underline link-underline-opacity-0" href="/">Home</a>
                    </li class="list-group-item ">
                    <li class="list-group-item bg-secondary border-0">
                        <a class="text-light link-underline link-underline-opacity-0" href="/dashboard">Dashboard</a>
                    </li>
                    <li class="list-group-item bg-secondary border-0">
                        {{#if logged_in}}
                        <a class="text-light link-underline link-underline-opacity-0" href="#" id="logout">logout</a>
                        {{else}}
                        <a class="text-light link-underline link-underline-opacity-0" href="/login">login</a>
                        {{/if}}
                    </li>
                </ul>
            </nav>
            <h1 class="bg-primary-subtle p-2 rounded shadow" style="width:fit-content">{{page}}</h1>
        </header>
        <main class="">
            <!-- Render the sub layout -->
            {{{ body }}}
        </main>
    </div>

    <!-- Render script for logged in users only -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>